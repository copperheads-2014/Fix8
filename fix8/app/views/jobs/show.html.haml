%h1
= render partial: "landlordbids" if @user.user_type == 'landlord'
= render partial: "contractor" if @user.user_type == 'contractor'

/ %div
/   = @job.description
/ %div
/   = link_to @job.landlord.name, @job.landlord
/ %ul
/   Bids on this job
/   - @job.recent_bids.each do |bid|
/     %li
/       = bid.contractor.name
/       = number_to_currency(bid.price)
/       = bid.created_at

/ = link_to "Place a new bid on this", new_job_bid_path(@job)
